node_modules/
.env
guilds.json
bans.jsonl
appeals.jsonl
bot.log
sessions.json
*.traineddata

# Deployment-only files (not part of the open-source distribution)
worker/
store.d1.js
d1-pending.jsonl
failover/
*.failover.*

# operator maintenance banner (deployment content)
notice.txt
costs.json
costs-history.jsonl

# deployment-only verification worker (edge-served Discord interactions)
verify-worker/
# deployment-only delegation runner (bot side of the edge worker; bot.js loads
# it only if present, so the base build stays server-only)
jobrunner.js
# deployment-only origin-guard worker (branded "we'll be back" page when the
# tunnel origin is down)
status-worker/
# deployment-only self-measuring D1 cost estimator
d1cost.js
